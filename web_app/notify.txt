무한 로딩 현상의 원인을 해결했습니다!

새로 생성한 Firestore 데이터베이스가 외부(웹 앱)에서 **데이터를 읽고 쓸 수 없도록 차단(Permission Denied)** 되어 있었습니다. 추가로 Next.js 서버 환경에서 호환성이 떨어지는 SDK 모듈 설정을 더 경량화된 `firestore/lite` 버전으로 교체했습니다.

제가 터미널을 통해 보안 규칙을 강제로 배포하고, 테스트 데이터(삼성전자 모의 기록)를 넣어보니 정상적으로 저장에 성공했습니다.

이제 앱에 들어가서 **"자산 추가"를 다시 시도해 보시면 즉각 반영되는 것을 보실 수 있습니다!**
혹시 방금 제가 넣은 삼성전자 10주(테스트용) 내역이 리스트에 보인다면, 관리 항목의 삭제 버튼을 눌러 먼저 지워주시고 새 거래를 넣어보세요. 작동 확인 후 `App Hosting`을 연동하시면 됩니다!
